import React from "react";
import CreateProjectForm from "./form";

export const metadata = {
  title: "Create Project | Emon's Portfolio",
};

const CreateProjectPage = () => {
  return (
    <div>
      <CreateProjectForm />
    </div>
  );
};

export default CreateProjectPage;
